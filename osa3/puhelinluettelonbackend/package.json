{
  "name": "puhelinluettelonbackend",
  "version": "1.0.0",
  "description": "puhelinluettelon backend",
  "license": "MIT",
  "author": "Miika Valkonen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd C:/Users/miika/code/OKT2 Full Stack Open/osa2/puhelinluettelo && npm run build && cp -r dist C:/Users/miika/code/OKT2 Full Stack Open/osa3/puhelinluettelonbackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
